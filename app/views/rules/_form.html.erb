  <%= form_for [@house, @rule || Rule.new], remote: true do |f| %>
    <%= f.text_area :content, :class => "rule-fields" %>
    <%= f.submit "Create Rule", :class => "button", :id => "rule-button"  %>
  <% end %>